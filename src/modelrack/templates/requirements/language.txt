# Base pinned dependencies for LLMs served via vLLM (e.g. Qwen3.6-35B-A3B).
# Installed into the model's isolated .venv by `modelrack setup`.
vllm>=0.6.3
torch>=2.5.0
transformers>=4.49.0
accelerate>=1.0.0
# --- server runtime ---
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
