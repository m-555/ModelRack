# Base pinned dependencies for text-to-audio (music / sound-effect) models.
# Installed into the model's venv by `modelrack setup`.
# Add the model's own inference package on top of these.
torch>=2.5.0
torchaudio>=2.5.0
soundfile>=0.12.1
einops>=0.7.0
safetensors>=0.4.5
transformers>=4.49.0
accelerate>=1.0.0
# --- server runtime ---
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
