# Base pinned dependencies for omni / any-to-any models (e.g. Qwen3-Omni).
# Installed into the model's isolated .venv by `modelrack setup`.
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0
transformers>=4.49.0
accelerate>=1.0.0
qwen-omni-utils>=0.0.4
soundfile>=0.12.1
pillow>=10.4.0
# flash-attn>=2.6.0            # recommended, needs matching CUDA toolchain
# --- server runtime ---
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
