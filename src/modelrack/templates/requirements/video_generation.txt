# Base pinned dependencies for video-generation models (e.g. WAN 2.2 I2V).
# Installed into the model's isolated .venv by `modelrack setup`.
torch>=2.5.0
torchvision>=0.20.0
diffusers>=0.32.0
transformers>=4.49.0
accelerate>=1.0.0
safetensors>=0.4.5
imageio>=2.35.0
imageio-ffmpeg>=0.5.1
ftfy>=6.2.0
# --- server runtime (kept in sync with modelrack templates) ---
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
