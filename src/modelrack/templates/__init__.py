"""Packaged data: server templates and base requirements copied into model folders.

These files are *not* imported by the hub. They are copied verbatim into each model's
folder by ``modelrack setup`` and run inside that model's isolated ``.venv``.
"""
