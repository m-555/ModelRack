# Chatterbox TTS — pinned deps for this model's venv.
torch>=2.5.0
torchaudio>=2.5.0
chatterbox-tts>=0.1.0
soundfile>=0.12.1
# perth (the TTS watermarker pulled in by chatterbox-tts) imports pkg_resources,
# which was removed from setuptools 81+ and is absent from minimal (uv) venvs.
setuptools<81
# server runtime
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
