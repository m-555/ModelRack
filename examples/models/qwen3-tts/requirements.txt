# Qwen3-TTS 12Hz (Custom Voice) — pinned deps for this model's isolated .venv.
torch>=2.5.0
torchaudio>=2.5.0
transformers>=4.49.0
accelerate>=1.0.0
soundfile>=0.12.1
# The server imports `from qwen_tts import Qwen3TTSModel`. Install the Qwen TTS
# package; if it is not on PyPI for your version, install from the official source:
#   pip install qwen-tts
#   # or: pip install "git+https://github.com/QwenLM/Qwen3-TTS.git"
qwen-tts
# Optional: flash-attn>=2.6.0 for attn_implementation=flash_attention_2 (else sdpa is used).
# server runtime
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pyyaml>=6.0
