version: "1.0"
models:

  # ── Video generation ────────────────────────────────────
  wan-2.2-i2v:
    type: video_generation
    backend: local
    config_path: wan-2.2-i2v/config.yaml
    tags: [video, i2v, diffusion, moe]
    added_at: "2026-07-02T00:00:00"

  # ── Image generation ────────────────────────────────────
  z-image-turbo:
    type: image_generation
    backend: local
    config_path: z-image-turbo/config.yaml
    tags: [image, diffusion, turbo, fast]
    added_at: "2026-07-02T00:00:00"

  qwen-image:
    type: image_generation
    backend: local
    config_path: qwen-image/config.yaml
    tags: [image, diffusion, qwen]
    added_at: "2026-07-02T00:00:00"

  # ── Image editing ───────────────────────────────────────
  qwen-image-edit:
    type: image_edit
    backend: local
    config_path: qwen-image-edit/config.yaml
    tags: [image, edit, diffusion, qwen]
    added_at: "2026-07-02T00:00:00"

  # ── Text-to-speech ──────────────────────────────────────
  chatterbox:
    type: tts
    backend: local
    config_path: chatterbox/config.yaml
    tags: [audio, tts, voice-cloning]
    added_at: "2026-07-02T00:00:00"

  qwen3-tts:
    type: tts
    backend: local
    config_path: qwen3-tts/config.yaml
    tags: [audio, tts, qwen, custom-voice]
    added_at: "2026-07-02T00:00:00"

  # ── Vision-language ─────────────────────────────────────
  qwen2.5-vl:
    type: vision_language
    backend: local
    config_path: qwen2.5-vl/config.yaml
    tags: [vision, language, multimodal, qwen]
    added_at: "2026-07-02T00:00:00"

  # ── Omni (any-to-any) ───────────────────────────────────
  qwen3-omni:
    type: omni
    backend: local
    config_path: qwen3-omni/config.yaml
    tags: [omni, multimodal, audio, vision, language, moe, qwen]
    added_at: "2026-07-02T00:00:00"

  # ── Language (LLM) ──────────────────────────────────────
  qwen3.6:
    type: language
    backend: local
    config_path: qwen3.6/config.yaml
    tags: [language, llm, moe, thinking, qwen]
    added_at: "2026-07-02T00:00:00"

  # ── Code ────────────────────────────────────────────────
  qwen3-coder:
    type: code
    backend: local
    config_path: qwen3-coder/config.yaml
    tags: [code, llm, coder, fp8, qwen]
    added_at: "2026-07-02T00:00:00"
